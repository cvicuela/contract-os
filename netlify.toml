[build]
  base    = "apps/web"
  command = "npm install && npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

[functions]
  external_node_modules = ["mammoth", "word-extractor"]

[[plugins]]
  package = "@netlify/plugin-nextjs"
